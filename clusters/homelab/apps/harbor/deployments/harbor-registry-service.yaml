apiVersion: v1
kind: Service
metadata:
  name: harbor-registry
  namespace: harbor
spec:
  selector:
    app: harbor-registry
  ports:
  - name: registry
    port: 5000
    targetPort: 5000
  - name: registryctl
    port: 8080
    targetPort: 8080
  type: ClusterIP