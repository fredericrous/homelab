# This is a template - apps should patch the service account name and role
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: vault-auth
spec:
  vaultConnectionRef: vault-connection
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: PLACEHOLDER  # Apps should patch this
    serviceAccount: PLACEHOLDER  # Apps should patch this
    audiences:
      - vault