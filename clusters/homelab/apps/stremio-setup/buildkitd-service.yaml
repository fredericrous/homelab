apiVersion: v1
kind: Service
metadata:
  name: buildkitd
  namespace: stremio
spec:
  type: ClusterIP
  ports:
  - port: 1234
    targetPort: 1234
    protocol: TCP
    name: buildkit
  selector:
    app: buildkitd