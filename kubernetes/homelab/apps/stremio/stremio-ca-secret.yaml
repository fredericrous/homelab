---
# ExternalSecret to create client CA secret for Stremio mTLS in istio-ingress
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: stremio-client-ca
  namespace: istio-ingress
spec:
  refreshInterval: 24h
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: stremio-cacert
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        ca.crt: '{{ .ca | toString }}'
        cacert: '{{ .ca | toString }}'
  data:
  - secretKey: ca
    remoteRef:
      key: secret/data/pki/ca
      property: ca.crt