apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: nextcloud-gateway
  namespace: nextcloud
spec:
  selector:
    istio: gateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - nextcloudx.daddyshome.fr
    - xdrivex.daddyshome.fr
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: MUTUAL
      credentialName: nextcloud-tls
    hosts:
    - nextcloudx.daddyshome.fr
    - xdrivex.daddyshome.fr
---
# Separate gateway for mobile access without mTLS
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: nextcloud-mobile-gateway
  namespace: nextcloud
spec:
  selector:
    istio: gateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - xdrive-mobilex.daddyshome.fr
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: SIMPLE  # No client certificate required for mobile
      credentialName: nextcloud-mobile-tls
    hosts:
    - xdrive-mobilex.daddyshome.fr
