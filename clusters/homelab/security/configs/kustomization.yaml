apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - vault-config-operator-configs.yaml
  # vault-config-operator/ resources are applied by vault-config-operator-configs.yaml Kustomization