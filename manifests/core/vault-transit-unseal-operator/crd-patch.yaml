# This ensures the CRD is always updated with the latest schema
# Including the postUnsealConfig field
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: vaulttransitunseals.vault.homelab.io
spec:
  conversion:
    strategy: None