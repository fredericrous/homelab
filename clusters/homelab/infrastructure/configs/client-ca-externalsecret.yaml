apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: client-ca-cert
  namespace: haproxy-ingress
spec:
  refreshInterval: 24h  # Check daily for CA updates
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend  # Sync from cluster Vault
  target:
    name: client-ca-cert
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        # HAProxy expects 'ca.crt' key for CA certificates
        ca.crt: '{{ .ca | toString }}'
  data:
  - secretKey: ca
    remoteRef:
      key: secret/data/pki/ca
      property: ca.crt