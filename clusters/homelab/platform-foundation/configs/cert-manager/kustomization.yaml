apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # ovh-credentials-secret.yaml moved to parent directory with reflector annotations
  - ovh-webhook-helm-release.yaml
  - ovh-webhook-rbac.yaml