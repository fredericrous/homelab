---
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: lldap-gateway
  namespace: lldap
spec:
  selector:
    istio: ingress-istio-gateway
  servers:
  # HTTP redirect to HTTPS
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - lldap.daddyshome.fr
  # HTTPS with mTLS client certificate authentication
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: MUTUAL
      credentialName: lldap-tls
    hosts:
    - lldap.daddyshome.fr
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: lldap-redirect
  namespace: lldap
spec:
  hosts:
  - lldap.daddyshome.fr
  gateways:
  - lldap-gateway
  http:
  # HTTP to HTTPS redirect for port 80
  - match:
    - port: 80
    redirect:
      scheme: https
      redirectCode: 301
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: lldap-https
  namespace: lldap
spec:
  hosts:
  - lldap.daddyshome.fr
  gateways:
  - lldap-gateway
  http:
  # HTTPS traffic to service (port 443)
  - match:
    - port: 443
    route:
    - destination:
        host: lldap
        port:
          number: 17170