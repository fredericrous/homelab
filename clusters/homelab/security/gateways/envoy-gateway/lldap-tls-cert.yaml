apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lldap-tls
  namespace: envoy-gateway-system
  labels:
    app.kubernetes.io/managed-by: manual-testing
spec:
  secretName: lldap-tls
  issuerRef:
    name: letsencrypt-ovh-webhook
    kind: ClusterIssuer
  dnsNames:
  - lldapx.daddyshome.fr