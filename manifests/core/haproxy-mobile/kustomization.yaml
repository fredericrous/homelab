apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: haproxy-mobile

resources:
  - namespace.yaml

helmCharts:
- name: haproxy-ingress
  repo: https://haproxy-ingress.github.io/charts
  version: 0.14.7
  namespace: haproxy-mobile
  releaseName: haproxy-mobile
  valuesFile: values.yaml