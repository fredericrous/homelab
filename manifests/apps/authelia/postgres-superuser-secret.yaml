apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: postgres-superuser-vault
  namespace: authelia
spec:
  type: kv-v2
  mount: secret
  path: postgres/superuser
  vaultAuthRef: default
  destination:
    name: postgres-superuser-ref
    create: true
    type: Opaque
    transformation:
      excludes:
        - .*
      templates:
        password:
          text: "{{ .Secrets.password }}"
  refreshAfter: 30s