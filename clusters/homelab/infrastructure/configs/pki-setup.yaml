---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pki-setup
  namespace: flux-system
spec:
  interval: 10m
  path: "./manifests/pki-setup"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: vault-transit-config
    - name: external-secrets-config
  postBuild:
    substituteFrom:
      - kind: Secret
        name: vault-admin-token