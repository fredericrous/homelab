apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: openwebui
spec:
  selector:
    istio: ingress-istio-gateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - aix.daddyshome.fr
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: MUTUAL
      credentialName: openwebui-tls
    hosts:
    - aix.daddyshome.fr
