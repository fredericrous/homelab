# BOOTSTRAP COMPONENT - Managed by applicationset-bootstrap.yaml
# This file is kept for reference but not used by applicationset-core.yaml
name: external-secrets-operator
project: core
namespace: external-secrets
path: manifests/core/external-secrets-operator
bootstrap: "true"  # Marks this as a bootstrap component
autoSync: "false"  # ESO is critical for secrets sync
prune: "false"     # Never auto-prune ESO resources
syncWave: "-15"    # Same priority as VSO (replacing it)
