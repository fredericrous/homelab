apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: build-stremio-web
  namespace: stremio
spec:
  entrypoint: build-stremio
  serviceAccountName: argo-workflow
  volumeClaimTemplates:
  - metadata:
      name: workspace
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 2Gi
      storageClassName: rook-ceph-block

  volumes:
  - name: docker-config
    secret:
      secretName: harbor-registry-creds

  templates:
  - name: build-stremio
    dag:
      tasks:
      - name: git-clone
        template: git-clone
      - name: check-image-exists
        template: check-image-exists
        dependencies: [git-clone]
      - name: build-and-push
        template: build-and-push
        dependencies: [check-image-exists]
        when: "'{{tasks.check-image-exists.outputs.parameters.result}}' == 'not-found'"

  - name: git-clone
    container:
      image: alpine/git:latest
      command:
      - sh
      - -c
      - |
        cd /workspace
        rm -rf ..?* .[!.]* *
        git clone https://github.com/Stremio/stremio-web.git .
        git checkout development
        ls -la
      volumeMounts:
      - name: workspace
        mountPath: /workspace

  - name: check-image-exists
    container:
      image: alpine:latest
      command:
      - sh
      - -c
      - |
        # Install required tools
        apk add --no-cache curl jq >&2
        
        # Copy docker config
        mkdir -p ~/.docker
        cp /docker-config/.dockerconfigjson ~/.docker/config.json
        
        # Extract credentials
        export HARBOR_USER=$(cat ~/.docker/config.json | jq -r '.auths["harbor-registry.harbor.svc.cluster.local:5000"].username')
        export HARBOR_PASS=$(cat ~/.docker/config.json | jq -r '.auths["harbor-registry.harbor.svc.cluster.local:5000"].password')
        
        # Check if image exists (try LoadBalancer IP for reliability)
        echo "Checking if image already exists in Harbor..." >&2
        if curl -f -s -u "$HARBOR_USER:$HARBOR_PASS" \
          "http://${ARGO_HARBOR_REGISTRY}/v2/library/stremio-web/manifests/latest" >/dev/null; then
          echo "Image already exists in Harbor." >&2
          echo "exists" > /tmp/result
        else
          echo "Image not found in Harbor." >&2
          echo "not-found" > /tmp/result
        fi
        
        # Output only the result to stdout
        cat /tmp/result
      volumeMounts:
      - name: docker-config
        mountPath: /docker-config
        readOnly: true
    outputs:
      parameters:
      - name: result
        valueFrom:
          path: /tmp/result

  - name: build-and-push
    container:
      image: moby/buildkit:latest
      command:
      - sh
      - -c
      - |
        # Setup authentication
        echo "Setting up authentication..."
        
        # Copy docker config for buildkit
        mkdir -p ~/.docker
        cp /docker-config/.dockerconfigjson ~/.docker/config.json
        export DOCKER_CONFIG=/root/.docker
        
        # Build and push
        echo "Building and pushing image..."
        buildctl --addr tcp://buildkitd.stremio:1234 build \
          --frontend dockerfile.v0 \
          --local context=/workspace \
          --local dockerfile=/workspace \
          --output type=image,name=harbor-registry.harbor.svc.cluster.local:5000/library/stremio-web:latest,push=true,registry.insecure=true \
          --registry-auth-tlscontext host=harbor-registry.harbor.svc.cluster.local:5000,insecure=true
      volumeMounts:
      - name: workspace
        mountPath: /workspace
      - name: docker-config
        mountPath: /docker-config
        readOnly: true
      env:
      - name: DOCKER_CONFIG
        value: /root/.docker