apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - cluster-config.yaml
  # Temporarily disabled - stuck in uninstalling state
  # - kyverno
  - cert-manager-setup.yaml
  - cert-manager-env-config.yaml
  - cert-manager
  - client-ca-externalsecret.yaml
  - lldap-istio-gateway.yaml
  - authelia-istio-gateway.yaml
  # Commented out - vault-admin-token should not be fetched from vault itself
  # - postgres-vault-token-secret.yaml
  - postgres-superuser-secret.yaml
  - postgres-db-creator-role.yaml
  - postgres-apps-cluster.yaml
  - postgres-setup.yaml
  - istio-cni-rbac-fix.yaml