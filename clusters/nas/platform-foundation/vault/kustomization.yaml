apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vault-helm-repo.yaml
  - vault-namespace.yaml
  - vault-helmrelease.yaml
  - vault-pv.yaml

patchesStrategicMerge:
  - vault-helmrelease-patch.yaml