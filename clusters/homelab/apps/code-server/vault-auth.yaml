apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: static-auth
  namespace: code-server
spec:
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: code-server
    serviceAccount: code-server
    audiences:
      - vault