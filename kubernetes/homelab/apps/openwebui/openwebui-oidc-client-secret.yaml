---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openwebui-oidc-client-secret
  namespace: openwebui
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: vault-backend
  target:
    name: openwebui-oidc-client-secret
    creationPolicy: Owner
  data:
    - secretKey: client_secret
      remoteRef:
        key: secret/data/openwebui
        property: OIDC_CLIENT_SECRET