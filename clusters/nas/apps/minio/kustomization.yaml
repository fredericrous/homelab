apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../platform-foundation/base
  - namespace.yaml
  - minio-helm-repo.yaml
  - minio-helmrelease.yaml
  - minio-values-configmap.yaml
  - minio-pv.yaml

patches:
  - minio-helmrelease-patch.yaml
