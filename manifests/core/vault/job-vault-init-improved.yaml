apiVersion: batch/v1
kind: Job
metadata:
  name: vault-init
  namespace: vault
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  backoffLimit: 3  # Retry up to 3 times if it fails
  template:
    spec:
      serviceAccountName: vault
      restartPolicy: OnFailure
      containers:
      - name: init-vault
        image: hashicorp/vault:1.20.1
        command:
        - /bin/sh
        - -c
        - |
          set -e
          
          # Install bash, jq, kubectl and curl
          echo "📦 Installing required tools..."
          apk add --no-cache bash jq kubectl curl
          
          # Script is already executable from configmap defaultMode
          # Execute the initialization script
          echo "🚀 Running Vault initialization script..."
          exec /scripts/vault-init.sh
        env:
        - name: VAULT_SKIP_VERIFY
          value: "true"
        - name: VAULT_ADDR
          value: "http://vault.vault.svc.cluster.local:8200"
        volumeMounts:
        - name: recovery
          mountPath: /recovery
        - name: init-script
          mountPath: /scripts
      volumes:
      - name: recovery
        configMap:
          name: vault-recovery
          optional: true
      - name: init-script
        configMap:
          name: vault-init-script
          defaultMode: 0755