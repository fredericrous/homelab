apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - cluster-secret-store.yaml
  - nas-vault-token-externalsecret.yaml
  - vault-pki-sync-externalsecret.yaml
  - cert-manager-ovh-credentials-externalsecret.yaml
  - client-ca-externalsecret.yaml
  - cluster-vars-pushsecret.yaml
  - nas-vault-token-vault-config-externalsecret.yaml
  # Vault policy configuration is now managed by vault-config-operator