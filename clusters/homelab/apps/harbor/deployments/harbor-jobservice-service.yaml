apiVersion: v1
kind: Service
metadata:
  name: harbor-jobservice
  namespace: harbor
spec:
  selector:
    app: harbor-jobservice
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP