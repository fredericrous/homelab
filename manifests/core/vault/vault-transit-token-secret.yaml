# This secret should contain the token from your QNAP Vault
# that has permission to use the transit/autounseal key
apiVersion: v1
kind: Secret
metadata:
  name: vault-transit-token
  namespace: vault
type: Opaque
stringData:
  token: "REPLACE_WITH_QNAP_VAULT_TOKEN"