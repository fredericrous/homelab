apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: stalwart-vault-secret
  namespace: stalwart
spec:
  type: kv-v2
  mount: secret
  path: stalwart
  destination:
    name: stalwart-vault-creds
    create: true
  refreshAfter: 30s
  vaultAuthRef: static-auth