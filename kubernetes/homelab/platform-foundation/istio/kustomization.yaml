apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - expose-services.yaml
  - expose-istiod.yaml
  - ca-bootstrap

patches:
- path: network-topology.yaml
  target:
    kind: Namespace
    name: istio-system
