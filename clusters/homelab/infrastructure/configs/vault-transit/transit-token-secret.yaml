apiVersion: v1
kind: Secret
metadata:
  name: vault-transit-token
  namespace: vault
type: Opaque
stringData:
  # TODO: Replace with actual transit token from CLAUDE.local.md
  token: "REPLACE_WITH_ACTUAL_TRANSIT_TOKEN"