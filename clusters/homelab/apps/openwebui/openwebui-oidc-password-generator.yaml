---
# Generate OIDC client secret for Open WebUI
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openwebui-oidc-client-secret-generator
  namespace: openwebui
spec:
  refreshInterval: 0  # Generate once
  secretStoreRef:
    kind: SecretStore
    name: vault-backend
  target:
    name: openwebui-oidc-client-secret-temp
    creationPolicy: Owner
  dataFrom:
  - sourceRef:
      generatorRef:
        apiVersion: generators.external-secrets.io/v1alpha1
        kind: Password
        name: openwebui-oidc-client-secret-generator
    rewrite:
    - regexp:
        source: "(.*)"
        target: "openwebui/$1"

---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: openwebui-oidc-client-secret-generator
  namespace: openwebui
spec:
  length: 64
  digits: 10
  symbols: 10
  symbolCharacters: "!@#$%^&*()_+-=[]{}|;:,.<>?"
  noUpper: false
  allowRepeat: true

---
# Push OIDC client secret to Vault for use by Open WebUI
apiVersion: external-secrets.io/v1
kind: PushSecret
metadata:
  name: push-openwebui-oidc-client-secret
  namespace: openwebui
spec:
  refreshInterval: 30s
  secretStoreRef:
    kind: SecretStore
    name: vault-backend
  selector:
    secret:
      name: openwebui-oidc-client-secret-temp
  data:
  - match:
      secretKey: openwebui
      remoteRef:
        remoteKey: secret/data/openwebui
        property: OIDC_CLIENT_SECRET