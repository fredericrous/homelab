# This secret will be created by the deployment script
# DO NOT commit this file with actual token values
# The deployment script will create this secret dynamically
#
# To manually create:
# kubectl create secret generic vault-transit-token \
#   --namespace=vault \
#   --from-literal=token=$K8S_VAULT_TRANSIT_TOKEN