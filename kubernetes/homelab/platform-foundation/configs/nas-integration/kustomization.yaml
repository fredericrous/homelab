apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - nas-token-external-secret.yaml
  - nas-token-broker-cronjob.yaml
  - envoy-circuit-breaker-config.yaml
  - vault-dns-anchor.yaml
  - vault-nas-service.yaml
  - nas-vault-service-entry.yaml
  # istio-remote-secret-nas is generated dynamically by bootstrap process
