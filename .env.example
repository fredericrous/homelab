# Homelab Environment Configuration
# Copy this to .env and adjust values for your environment

# ============================================
# ArgoCD Variables (prefixed with ARGO_)
# These are used for manifest templating
# ============================================
ARGO_CONTROL_PLANE_IP=192.168.1.67  # Must match terraform nodes.controlplane.ip
ARGO_NAS_VAULT_ADDR=http://YOUR-NAS-IP:61200
ARGO_EXTERNAL_DOMAIN=yourdomain.com
ARGO_CLUSTER_NAME=homelab
ARGO_CLUSTER_DOMAIN=cluster.local
ARGO_HARBOR_IP=192.168.1.90      # IP for Harbor LoadBalancer service
ARGO_HARBOR_REGISTRY=harbor.yourdomain.com  # Harbor registry URL
ARGO_HARBOR_REGISTRY_TLS=true     # Whether Harbor uses TLS

# ============================================
# Local/Script Variables (not exposed to ArgoCD)
# These are used by deployment scripts
# ============================================

# QNAP/NAS Configuration
QNAP_VAULT_ADDR=http://YOUR-NAS-IP:61200
QNAP_VAULT_TOKEN=  # Your QNAP Vault token or root token

# Vault Transit Configuration
VAULT_TRANSIT_TOKEN=  # Transit token from NAS Vault for auto-unsealing cluster Vault

# FluxCD Configuration
FLUXCD_GITHUB_TOKEN=  # GitHub personal access token for FluxCD
FLUXCD_OWNER=fredericrous  # GitHub owner/organization name
FLUXCD_REPOSITORY=homelab  # GitHub repository name

# OVH DNS Configuration (for Let's Encrypt)
# These are stored in Vault and accessed via ESO
OVH_APPLICATION_KEY=your-application-key
OVH_APPLICATION_SECRET=your-application-secret
OVH_CONSUMER_KEY=your-consumer-key

# Plex Configuration
PLEX_CLAIM_TOKEN=  # Get from https://www.plex.tv/claim/

# Storage classes (currently unused - can be enabled when needed)
# STORAGE_CLASS_DEFAULT=rook-cephfs
# STORAGE_CLASS_BLOCK=rook-ceph-block

# Common namespaces (currently unused - can be enabled when needed)
# VAULT_NAMESPACE=vault
# MONITORING_NAMESPACE=monitoring

# Common service endpoints (currently unused - can be enabled when needed)
# VAULT_ADDR=http://vault.vault.svc.cluster.local:8200
# POSTGRES_HOST=postgres-cluster.cloudnative-pg.svc.cluster.local

# Resource defaults (currently unused - can be enabled when needed)
# DEFAULT_REPLICAS=1
# DEFAULT_CPU_REQUEST=50m
# DEFAULT_MEMORY_REQUEST=128Mi
# DEFAULT_CPU_LIMIT=500m
# DEFAULT_MEMORY_LIMIT=512Mi

# Optional: Override default IPs
# CONTROL_PLANE_IP=192.168.1.67
# WORKER1_IP=192.168.1.68
# WORKER2_IP=192.168.1.69
