apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ca-certificates-pvc
  namespace: authelia
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 1Gi