apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minio

resources:
  - minio-standalone.yaml
  - minio-service.yaml
  - minio-external-secret.yaml
  - virtualservice.yaml
  - job-init-bucket.yaml