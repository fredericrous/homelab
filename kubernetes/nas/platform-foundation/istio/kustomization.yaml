apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - istio-helm-repo.yaml
  - istio-base.yaml
  - istiod.yaml
  - ztunnel.yaml
  - istio-eastwest-gateway.yaml
  - expose-services.yaml
  - expose-istiod.yaml

patches:
- path: network-topology.yaml
  target:
    kind: Namespace
    name: istio-system
