apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: istio-system

resources:
  - istio-ca-bootstrap-job.yaml
  - istio-ca-vault-backup.yaml
  - nas-kubeconfig-secret.yaml
  - istio-ca-export-job.yaml
