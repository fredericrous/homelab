apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This base provides common backup patches
# Apps can include this and specify which volumes to backup

patches:
  - path: deployment-backup-patch.yaml
    target:
      kind: Deployment
  - path: statefulset-backup-patch.yaml
    target:
      kind: StatefulSet