apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argocd

resources:
  - argocd-vault-plugin-config.yaml
  - argocd-vault-plugin-secret.yaml

# The patch will be applied by ArgoCD's self-management
# after the repo-server deployment exists