apiVersion: v1
kind: Service
metadata:
  name: harbor-proxy
  namespace: harbor
spec:
  selector:
    app: harbor-proxy
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: https
    port: 443
    targetPort: 8443
  type: ClusterIP