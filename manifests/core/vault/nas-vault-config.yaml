# ConfigMap with transit vault configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: nas-vault-config
  namespace: vault
data:
  config.yaml: |
    transit:
      address: "PLACEHOLDER_NAS_VAULT_ADDR"
      mountPath: "transit"
      keyName: "autounseal"
      tlsSkipVerify: true
