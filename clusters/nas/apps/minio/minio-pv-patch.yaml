---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nas-local-pv
  labels:
    app: minio
    type: local
spec:
  storageClassName: nas-local
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /share/VMs/kubernetes/minio
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists