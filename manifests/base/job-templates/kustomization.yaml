apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - job-vault-configure-v2.yaml
  - job-vault-populate-v2.yaml
  - job-secret-copier.yaml
  # - job-vault-configure-template.yaml  # Replaced by v2
  # - job-vault-populate-template.yaml  # Replaced by v2
  # - job-db-init-template.yaml  # TODO: Create v2 version

patches:
  - path: security-context-patch.yaml